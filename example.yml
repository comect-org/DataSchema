types:
  - name: FirstType
    comment: >
      The first type.
      
      With two lines in the comment.

    attributes:
      - name: one
        type: MutableMap
        comment: Map strings to strings.

        parameters:
          - type: String
          - type: String

      - name: two
        type: String
        comment: Number two.

      - name: three
        type: ImmutableList

        parameters:
          - type: SecondType

settings:
  package: com.example.test
  languages: [kotlin]
